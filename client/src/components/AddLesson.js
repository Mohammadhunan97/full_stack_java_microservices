import React from 'react';

const AddLesson = () => (
    <div>
        <h1>Add a new lesson:</h1>
        <form>
            <input placeholder="title" />
`           <input type="submit"/>
        </form>
    </div>
);

export default AddLesson;